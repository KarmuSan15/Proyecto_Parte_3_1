import { Canvas, Meta } from "@storybook/blocks"; // Importamos componentes de Storybook
import Example from "./example.tsx";// Importamos el componente Example
import * as ToruhDLStories from "./ToruhDL.stories"; // Importamos las historias de ToruhDL

<Meta of={ToruhDLStories} title="Components/ToruhDL" /> // Definimos los metadatos del componente ToruhDL para Storybook

# ToruhDL
// Descripción del componente: Es un componente que muestra información de un desarrollador con opciones de contacto y enlaces a redes sociales.

#### Ejemplo
<Canvas of={ToruhDLStories.Default} /> {/* Muestra visualmente el ejemplo del componente ToruhDL en Storybook */}

## Uso
```tsx
import ToruhDL from 'milibreria'; // Importamos el componente ToruhDL desde la librería externa 'milibreria'

function Example() {
  return (
    <ToruhDL
      name="Toruh Delgado Lahera" // Nombre del desarrollador
      description="Desarrollador Web y Entusiasta de la Tecnología." // Descripción del desarrollador
      avatarUrl="https://i.pinimg.com/736x/88/e9/aa/88e9aa944c9bc14936d6e8cf7c954473.jpg" // URL del avatar del desarrollador
    />
  );
}

export default Example; // Exportamos el componente Example para su uso../example.tsx
