import { Canvas, Meta } from "@storybook/blocks"; // Importamos componentes de Storybook
import Example from "./example.tsx"; // Importamos el componente Example
import * as MyFavouriteBorder from "./MyFavouriteBorder.stories"; // Importamos las historias de MyFavouriteBorder

<Meta of={MyFavouriteBorder} title="MyFavouriteBorder" /> // Definimos los metadatos del componente MyFavouriteBorder para Storybook, incluyendo el título

# MyFavouriteBorder
// Descripción del componente: Es un botón de favorito con borde, que permite marcar elementos como favoritos.

#### Example
<Canvas of={MyFavouriteBorder.Default} /> {/* Muestra visualmente el ejemplo del botón por defecto en Storybook */}

## Uso
```tsx
import { MyFavouriteBorder } from 'milibreria'; // Importamos el componente MyFavouriteBorder desde la librería externa 'milibreria'

function Example() {
  return (
    <MyFavouriteBorder
      color="red" // Color del icono
      disabled={false} // Indica si el botón está habilitado
      onClick={() => alert("¡Botón de favorito clicado!")} // Función que se ejecuta al hacer clic
    />
  );
}

export default Example; // Exportamos el componente Example para su uso
