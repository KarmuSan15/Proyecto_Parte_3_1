import { Canvas, Meta } from "@storybook/blocks"; // Importamos componentes de Storybook
import * as MyFavouriteBorderStories from "./MyFavouriteBorder.stories"; // Importamos las historias de MyFavouriteBorder

// Metadatos de MyFavouriteBorder
<Meta of={MyFavouriteBorderStories} title="MyFavouriteBorder" />

# MyFavouriteBorder
Componente que permite marcar elementos como favoritos con un borde.

#### Example - Default
<Canvas of={MyFavouriteBorderStories.Default} /> {/* Visualización del estado por defecto */}

#### Example - Disabled
<Canvas of={MyFavouriteBorderStories.Disabled} /> {/* Visualización del estado deshabilitado */}

## Uso
```tsx
import { MyFavouriteBorder } from 'milibreria';

function Example() {
  return (
    <MyFavouriteBorder
      color="blue"
      disabled={false}
      onClick={() => alert("¡Botón de favorito clicado!")}
    />
  );
}


