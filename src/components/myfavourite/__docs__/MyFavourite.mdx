import { Canvas, Meta } from "@storybook/blocks";
import Example from "./example.tsx";
import * as MyFavouriteStories from "./MyFavourite.stories.tsx";

<Meta of={MyFavouriteStories} title="Components/MyFavourite" />

# MyFavourite
Componente que permite marcar elementos como favoritos.

#### Example-Primary
<Canvas of={MyFavouriteStories.Primary} />

#### Example-Secondary
<Canvas of={MyFavouriteStories.Secondary} />

## Usage
```tsx
import { MyFavourite } from 'milibreria';

function Example() {
  return (
    <MyFavourite
      color="red"
      disabled={false}
      onClick={() => alert("¡Icono clicado!")}
    />
  );
}


